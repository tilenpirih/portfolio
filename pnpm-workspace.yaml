shellEmulator: true

# No `trustPolicy: no-downgrade` on purpose: it rejects chokidar@4.0.3 (pulled in
# transitively via @netlify/blobs) for lacking provenance, which blocks install.
# The pnpm/yaml-enforce-settings ESLint rule is off to match.

packages:
  - .

patchedDependencies:
  nuxt-particles@0.3.0: patches/nuxt-particles@0.3.0.patch
